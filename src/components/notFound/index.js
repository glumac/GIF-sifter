import React from 'react';
import { NotFoundWrap, NotFoundHeading } from './styles';

const NotFound = props => {
  return (
    <NotFoundWrap>
      <NotFoundHeading>Not &nbsp;&nbsp; Found!!</NotFoundHeading>
    </NotFoundWrap>
  );
};

export default NotFound;
